import { FlvMuxer } from "./core/flv-muxer";
import { test } from "./core/muxer";

const metadata = {
    duration: 120.5,
    width: 640,
    height: 480,
    videodatarate: 500,
    framerate: 30,
    videocodecid: 7,
    audiodatarate: 128,
    audiocodecid: 10,
};

export function init() {
    
}

init();

test();